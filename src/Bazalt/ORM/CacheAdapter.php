<?php

namespace Bazalt\ORM;

class CacheAdapter
{
    public function removeByTags($tags = array())
    {
        //Cache::Singleton()->removeByTags($this->getCacheTags());
    }

    public function get($key)
    {
        //Cache::Singleton()->removeByTags($this->getCacheTags());
    }

    public function set($key, $value, $lifeTime = 0, $tags = array())
    {
        //Cache::Singleton()->removeByTags($this->getCacheTags());
    }

    public function defaultLifeTime()
    {
        //Cache::Singleton()->removeByTags($this->getCacheTags());
    }
}